package com.example.personneservice.enumeration;

public enum ErrorMessage {
    KEY_ERROR("la force n'est pas avec toi !"), AGE_ERROR("vous avez moin de 18 ans");

    ErrorMessage(String s) {
    }
}
